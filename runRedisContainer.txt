# for linking to other containers...
sudo docker run -d --name rediscontainer -e REDIS_PASS="com" tutum/redis

# for linking to the outside WORLD (or maybe just localhost!) and other containers (for sure)!!...
sudo docker run -d -p 0.0.0.0:6379:6379 --name rediscontainer -e REDIS_PASS="com" tutum/redis

# for linking to the outside WORLD (or maybe just localhost!) and other containers (for sure)!!...PLUS last recently used memmory-use limiting
sudo docker run -d -p 0.0.0.0:6379:6379 --name rediscontainer -e REDIS_PASS="com" -e REDIS_MODE="LRU" -e REDIS_MAXMEMORY="256mb" tutum/redis


